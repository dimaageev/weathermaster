import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    width: "70%",
    height: 140,
    justifyContent: "space-around",
    alignItems: "center",
    borderRadius: 15,
  },
});
