import { StyleSheet } from "react-native";

export default StyleSheet.create({
  flatlistContainer: {
    flexGrow: 1,
    justifyContent: "center",
    alignItems: "center",
    marginHorizontal: 30,
  },
});
